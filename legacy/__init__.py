"""
legacy/ — pipeline multi-agente histórico que llamaba a la API de Anthropic.

Se conserva como referencia. NO forma parte del flujo nuevo (estilo Text2CAD), donde el
cerebro es el LLM externo manejando el CLI `qpot`. Estos módulos requieren `pip install
anthropic` y una API key para funcionar; ya no se importan por defecto.
"""
